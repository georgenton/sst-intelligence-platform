# Contributing

Use Node 24 and pnpm 10. Create a branch, implement one vertical slice, add tests and documentation,
then run `pnpm check`. Commits follow Conventional Commits. Do not commit `.env` files, generated
build output, personal data, or credentials. Database changes require a reviewed Prisma migration.
